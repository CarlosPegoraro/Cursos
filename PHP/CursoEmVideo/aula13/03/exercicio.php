<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="../../_css/estilo.css">
    <title>Curso de PHP</title>
</head>
<body>
    <div>
        <?php
              /*strlen - conta o numero de CARACTERES em uma string<br/>
              trim - recorta espaços ANTES das palavras e no FINAL das palavras<br/>
              ltrim - recorta os espaços ANTES das palavras<br/>
              rtrim - recorta somente os espaços no FINAL<br/>
              str_word_count - conta quantas PALAVRAS tem em uma string (0 - so conta; 1 - cria um array; 2 - mostra onde comeca caa palavra em um array<br/>
              explode - versão nova do str_word_count usando o parametro 1(colocar o caracter de separacao entre aspas , string que vai ser explodida<br/>
              str_split - pega cada letra e coloca em um array<br/>
              implode - pega um vetor(array) e transforma em uma string(colocar o caracter de separacao entre aspas , vetor que vai ser unido<br/>
              chr - mostra qual letra é representada por aquele codigo chr(69)<br/>
              ord - faz o contrario do chr<br/>
              strtolower - transforma todos os caracteres em minúsculo<br/>
              strtoupper - transforma todos os caracteres em maiúsculo<br/>
              ucfirst - coloca a primeira letra da string em maiúscula<br/>
              ucwords - coloca a primeira letra de cada palavra em maiúscula<br/>
              strrev - mostra a string de traz pra frente<br/>
              strpos - mostra em qual posicao um determinado termo comeca (texto, termo)<br/>
              stripos - mesma coisa que a strpos, porem ele ignora letras maiúsculas, le como tudo minúsculo<br/>
              substr_count - conta quantas vezes um mesmo termo aparece em uma string (texto, termo)<br/>
              substr - recorta um pedaço da string (texto, posicao inicial do recorte, posicao final do recorte<br/>
              str_pad - faz uma string caber em um determinado tamanho (texto, tamanho, caracter para preencher, lado que eh para ser preenchido 'str_pad_(lado)'<br/>
              str_repeat - repete uma string um determinado numero de vezes (termo, quantas vezes vai repitir)<br/>
              str_replace - muda um termo para outro (termo antigo, termo novo, texto)<br/>
              str_replace - mesma coisa que o str_replace mas ignora maiúsculas<br/>"*/
        ?>
    </div>
</body>
</html>